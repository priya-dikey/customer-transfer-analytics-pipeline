# Git install (works without the dbt hub). The hub form also works if your
# network allows hub.getdbt.com:
#   - package: dbt-labs/dbt_utils
#     version: [">=1.1.0", "<2.0.0"]
packages:
  - git: "https://github.com/dbt-labs/dbt-utils.git"
    revision: 1.3.0
