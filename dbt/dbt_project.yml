name: 'high_value_segmentation'
version: '1.0.0'
config-version: 2

profile: 'high_value_segmentation'

model-paths: ["models"]
test-paths: ["tests"]

models:
  high_value_segmentation:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
