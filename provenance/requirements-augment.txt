# Dependencies for provenance/augment_transfers.py and load_to_warehouse.py
# only. Not required to run the dbt project.
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
duckdb>=0.10
# load_to_warehouse.py --target postgres additionally needs:
# sqlalchemy>=2.0
# psycopg2-binary>=2.9
